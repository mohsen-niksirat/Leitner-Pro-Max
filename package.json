{
  "name": "leitner-pro-max",
  "version": "5.0.0",
  "private": true,
  "description": "آزمون‌های مرورگر پروژه لایتنر پرو مکس (GitHub Pages-ready)",
  "scripts": {
    "test": "playwright test",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "playwright test",
    "syntax": "node --check js/app.js && node --check js/core/boot.js && node --check js/learning/fsrs.js && node --check js/ai/ai-manager.js",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
